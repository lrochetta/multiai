# Story 3.1 — Documentation utilisateur dans `docs/` (VitePress)

**Sprint :** 🟡 3 — MEDIUM
**Priorité :** MEDIUM
**Effort :** 3h
**Agent assigné :** Huldah (Docs)
**Dépendances :** S2.2 (YAML/hooks câblés avant doc)
**Fichiers :** `multiai-go/docs/`

---

## Objectif

Le dossier `docs/` est vide. Créer un site VitePress complet.

## Pages à créer

```
docs/
├── index.md                    ← "Routeur multi-IA sécurisé"
├── .vitepress/config.ts        ← Configuration VitePress
├── guide/
│   ├── getting-started.md      ← npm install, premier lancement, wizard
│   ├── profiles.md             ← Format .env, champs (PROFILE_ID, TOOL, etc.)
│   ├── configuration.md        ← Configurer les clés, credential store
│   ├── fallback.md             ← Chaînes de fallback, FALLBACK=
│   ├── openrouter.md           ← models, search, compare, Fusion
│   └── troubleshooting.md      ← Erreurs courantes + solutions
├── reference/
│   ├── commands.md             ← Toutes les sous-commandes + flags
│   ├── env-variables.md        ← MULTIAI_*, CLEAR_ENV, etc.
│   ├── providers.md            ← Les 13 fournisseurs + shortcuts
│   └── exit-codes.md           ← 0, 1, 2, 3, 4, 130
└── advanced/
    ├── custom-profiles.md      ← Créer son profil .env / .yaml
    ├── project-config.md       ← .multiai.yaml
    ├── hooks.md                ← before_launch / after_launch
    └── credential-store.md     ← Architecture AES-256-GCM
```

## Setup technique

```bash
cd multiai-go/docs
npm init -y
npm install -D vitepress
npx vitepress init
```

## Contenu minimum par page

Chaque page doit contenir :
- Titre + description
- Exemple de code fonctionnel (copier-coller)
- Résultat attendu
- Pièges courants

## Résultat attendu

```
✅ Site VitePress fonctionnel
✅ 13 pages minimum
✅ Chaque page a un exemple exécutable
✅ Déploiement GitHub Pages configuré
```

## Sortie de story

- `docs/` peuplé avec VitePress
- `npm run docs:dev` fonctionnel
- README mis à jour avec lien vers la doc
