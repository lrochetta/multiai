# Story 3.2 — Tests pour les packages à 0% et <30%

**Sprint :** 🟡 3 — MEDIUM
**Priorité :** MEDIUM
**Effort :** 4h
**Agent assigné :** Sentinel (QA)
**Dépendances :** S2.7 (display extrait avant test menu)
**Fichiers :** nouveaux `*_test.go`

---

## Objectif

Éliminer les packages à 0% de couverture et remonter ceux sous 30%.

## Cibles

### Package `internal/fsutil` (0% → 80%)

```go
func TestWriteFileAtomic_Success(t *testing.T) {
    dir := t.TempDir()
    path := filepath.Join(dir, "test.txt")
    err := WriteFileAtomic(path, []byte("hello"), 0644)
    if err != nil { t.Fatal(err) }
    content, _ := os.ReadFile(path)
    if string(content) != "hello" { t.Error("content mismatch") }
}

func TestWriteFileAtomic_NoPartialWrite(t *testing.T) {
    // Vérifier qu'en cas d'erreur, le fichier original est préservé
}

func TestWriteFileAtomic_PermissionDenied(t *testing.T) {
    // Sur un dossier en lecture seule
}

func TestWriteFileAtomic_LargeFile(t *testing.T) {
    // 10 MB
}
```

### Package `internal/menu` (14% → 50%)

```go
func TestSelectTool_ChooseFirst(t *testing.T) {
    input := "1\n"  // choisit le premier outil
    oldStdin := os.Stdin
    r, w, _ := os.Pipe()
    os.Stdin = r
    defer func() { os.Stdin = oldStdin }()
    
    go func() {
        w.Write([]byte(input))
        w.Close()
    }()
    
    tools := []cli.ToolInfo{{Name: "Claude Code"}, {Name: "Codex CLI"}}
    selected, err := SelectTool(tools)
    // ...
}

func TestCountSecrets_AllSet(t *testing.T) { ... }
func TestCountSecrets_NoneSet(t *testing.T) { ... }
func TestCountSecrets_Partial(t *testing.T) { ... }
```

### Package `internal/logging` (48% → 70%)

```go
func TestLogger_Debug(t *testing.T) { ... }
func TestLogger_Info(t *testing.T) { ... }
func TestLogger_Warn(t *testing.T) { ... }
func TestLogger_Error(t *testing.T) { ... }
func TestLogger_MinLevel(t *testing.T) { ... }
```

### Package `internal/cli` (54% → 70%)

```go
func TestEscapeShellArg(t *testing.T) {
    tests := []struct{
        input, expected string
    }{
        {"hello", "hello"},
        {"hello world", "\"hello world\""},
        {"it's", "\"it's\""},
        {`has"quote`, `"has\"quote"`},
    }
    // ...
}

func TestRunBeforeHooks_EmptyList(t *testing.T) { ... }
func TestRunAfterHooks_NoShell(t *testing.T) { ... }
```

## Résultat attendu

```
✅ internal/fsutil : 0% → ≥80%
✅ internal/menu : 14% → ≥50%
✅ internal/logging : 48% → ≥70%
✅ internal/cli : 54% → ≥70%
✅ Couverture globale : ~50% → ≥65%
```

## Sortie de story

- ≥ 15 nouveaux tests
- Plus aucun package à 0%
- Couverture globale ≥ 65%
