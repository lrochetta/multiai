# 2026-07-20 — v0.7.0 : fournisseur NVIDIA + pont Anthropic→OpenAI intégré — RELEASED

## Résultat

`multiai@0.7.0` est **GitHub + npm latest**, vérifié sur le registry. GLM 5.2 (et 117 autres modèles gratuits de build.nvidia.com) utilisables dans Claude Code **sans aucun proxy externe**.

## Livré

1. **Provider `nvidia`** (14ᵉ) : catalog, clé `NVIDIA_API_KEY` (console <https://build.nvidia.com/settings/api-keys>), 3 profils embarqués (40 au total) : `nv-cc` (Claude Code, pont intégré), `codex-nv` (Codex via LiteLLM), `ocnvidia` (OpenCode direct).
2. **`internal/bridge`** : pont Anthropic Messages ↔ OpenAI chat/completions in-process (SSE, tool calls avec deltas sans index et métadonnées tardives, reasoning→thinking, `stream_options.include_usage`, erreurs mi-stream en événement `error`, `stop_reason` dérivé des tool calls, count_tokens, /v1/models, clamp 32768). Cycle de vie par le launcher via métadonnées `BRIDGE_*` ; clé backend jamais dans l'env enfant ; standalone `multiai bridge` (:4100).
3. **Menu « 5. NVIDIA »** : découverte (cache 1 h, fallback embarqué, backend factorisé avec OpenRouter), profils dynamiques `nv-*`/`98-*.env` avec garde anti-collision de shortcuts.
4. **Réparation Codex 2026** (prouvée au fil, listener local + codex-cli 0.144.6) : `OPENAI_BASE_URL` ignoré → fallback silencieux sur le compte OpenAI connecté. `codex-qwen`/`req-codex` réparés (flags `-c`, `wire_api=responses`, backends vérifiés par probe différentiel 401-vs-404) ; `codex-sf` [CASSE] (pas d'API Responses chez SiliconFlow).
5. **Docs** : guide `docs/guide/nvidia.md`, providers/env-variables/profiles réécrits sur les 40 profils réels, sidebar VitePress, READMEs, CHANGELOG 0.7.0.

## Qualité

- 2 revues adversariales (28 + 43 agents) : 18 findings confirmés, tous corrigés.
- Tests Go complets + 40 tests npm verts ; CI master 19/19 ; validation live : stream usage réel 15/4, E2E `multiai launch -p nv-cc` → `claude -p` → GLM répond.

## Release ops (voir decisions.md 2026-07-20)

Tag `v0.7.0` → env protégé `release` approuvé via API → GoReleaser 11 assets (AUR en échec connu) → relogin npm web (`npm-auth-type: web`) → publish WebAuthn en `--json` piloté Node avec `npm_config_otp`. Protection master restaurée (9 checks).

## Suivis

- [ ] laurent : rotation clé nvapi (collée en conversation) ; réactiver Avast (+ exclusion build Go).
- [ ] Smoke `npx multiai@latest version` sur poste vierge.
- [ ] Moderniser `actions/upload-artifact` (dépréciation Node 20).
- [ ] Éventuel : étendre le pont intégré à l'API Responses → suppression de LiteLLM pour Codex.
